# 紫微斗數網站部署教學

本文件將教您如何將紫微斗數計算網站部署到 **GitHub Pages** 和 **Cloudflare Pages**，讓網站可以在網路上公開存取。

## 📋 目錄

- [方案比較](#方案比較)
- [方案一：GitHub Pages 部署](#方案一github-pages-部署)
- [方案二：Cloudflare Pages 部署](#方案二cloudflare-pages-部署)
- [部署後設定](#部署後設定)
- [常見問題](#常見問題)

---

## 方案比較

| 特性 | GitHub Pages | Cloudflare Pages |
|------|--------------|------------------|
| 免費額度 | ✅ 完全免費 | ✅ 完全免費 |
| 速度 | 🟡 中等 | ✅ 非常快（全球 CDN） |
| 設定難度 | ✅ 簡單 | 🟡 中等 |
| 自訂網域 | ✅ 支援 | ✅ 支援 |
| 建置時間 | 🟡 較慢 | ✅ 快速 |
| SSL 憑證 | ✅ 自動 | ✅ 自動 |

**建議：**
- 🎯 **新手推薦**：GitHub Pages（設定最簡單）
- ⚡ **追求速度**：Cloudflare Pages（全球 CDN 加速）

---

## 方案一：GitHub Pages 部署

### 步驟 1：建立 GitHub 帳號

如果還沒有 GitHub 帳號：
1. 前往 [https://github.com/](https://github.com/)
2. 點選「Sign up」註冊
3. 完成註冊流程

### 步驟 2：建立新的 Repository

1. 登入 GitHub 後，點選右上角的 `+` → **New repository**
2. 設定如下：
   - **Repository name**：`ziwei-calculator`（或您喜歡的名稱）
   - **Description**：紫微斗數命盤計算系統
   - **Public**：✅ 選擇 Public（免費的 GitHub Pages 需要公開）
   - ❌ **不要**勾選「Add a README file」
3. 點選 **Create repository**

### 步驟 3：上傳檔案到 GitHub

#### 方法 A：使用指令（推薦）

在您的專案資料夾中開啟終端機（Terminal），執行：

```bash
cd /Users/donpanic/Documents/programming/website/ZW_Cal

# 初始化 git（如果還沒有）
git init

# 加入所有檔案
git add .

# 建立第一個 commit
git commit -m "初始化紫微斗數網站"

# 連結到您的 GitHub repository（記得改成您的使用者名稱）
git remote add origin https://github.com/您的使用者名稱/ziwei-calculator.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

#### 方法 B：使用 GitHub 網頁介面

1. 在 GitHub repository 頁面，點選 **uploading an existing file**
2. 拖曳以下檔案到上傳區：
   - `index.html`
   - `result.html`
   - `lunar.js`
   - `ziweistar.js`
   - `ziweicore.js`
   - `ziweiui.js`
   - `google-sheets-integration.js`
   - `js/ziwei.css`（需要先建立 `js` 資料夾）
3. 點選 **Commit changes**

### 步驟 4：啟用 GitHub Pages

1. 在您的 repository 頁面，點選 **Settings**（設定）
2. 在左側選單找到 **Pages**
3. 在 **Source** 區塊：
   - Branch：選擇 **main**
   - Folder：選擇 **/ (root)**
4. 點選 **Save**
5. 等待 1-2 分鐘，頁面會顯示：
   ```
   Your site is live at https://您的使用者名稱.github.io/ziwei-calculator/
   ```

### 步驟 5：測試網站

開啟瀏覽器，前往：
```
https://您的使用者名稱.github.io/ziwei-calculator/index.html
```

✅ 完成！您的網站已經上線了！

---

## 方案二：Cloudflare Pages 部署

### 步驟 1：建立 Cloudflare 帳號

1. 前往 [https://dash.cloudflare.com/sign-up](https://dash.cloudflare.com/sign-up)
2. 註冊帳號（建議使用 Google 帳號快速登入）
3. 完成郵件驗證

### 步驟 2：準備 GitHub Repository

⚠️ **前提**：需要先完成「方案一」的步驟 1-3，將程式碼上傳到 GitHub。

### 步驟 3：建立 Cloudflare Pages 專案

1. 登入 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 點選左側選單 **Workers & Pages**
3. 點選 **Create application** → **Pages** → **Connect to Git**
4. 選擇 **GitHub**，授權 Cloudflare 存取您的 GitHub 帳號
5. 選擇您的 repository：`ziwei-calculator`

### 步驟 4：設定建置選項

1. **Project name**：`ziwei-calculator`（或自訂）
2. **Production branch**：`main`
3. **Build settings**：
   - Framework preset：**None**
   - Build command：留空
   - Build output directory：`/`
4. 點選 **Save and Deploy**

### 步驟 5：等待部署完成

- 部署過程約 30 秒到 1 分鐘
- 完成後會顯示：
  ```
  Success! Your site is live at https://ziwei-calculator.pages.dev
  ```

### 步驟 6：測試網站

開啟瀏覽器，前往：
```
https://ziwei-calculator.pages.dev/index.html
```

✅ 完成！您的網站已經在 Cloudflare 上線了！

---

## 部署後設定

### 自訂網域名稱（選用）

#### GitHub Pages

1. 在 repository 的 **Settings** → **Pages**
2. 在 **Custom domain** 輸入您的網域（例如：`ziwei.example.com`）
3. 在您的網域 DNS 設定中新增 CNAME 記錄：
   ```
   ziwei.example.com → 您的使用者名稱.github.io
   ```

#### Cloudflare Pages

1. 在 Cloudflare Pages 專案頁面，點選 **Custom domains**
2. 點選 **Set up a custom domain**
3. 輸入您的網域名稱
4. Cloudflare 會自動設定 DNS（如果網域也在 Cloudflare 管理）

### 更新網站內容

#### 使用 Git 指令

```bash
# 修改檔案後

# 加入變更
git add .

# 建立 commit
git commit -m "更新網站內容"

# 推送到 GitHub
git push
```

- **GitHub Pages**：約 1-3 分鐘後自動更新
- **Cloudflare Pages**：約 30 秒後自動更新

#### 使用 GitHub 網頁

1. 前往 GitHub repository
2. 點選要修改的檔案
3. 點選鉛筆圖示（編輯）
4. 修改後點選 **Commit changes**
5. 等待自動部署完成

---

## 常見問題

### Q1: 為什麼 Google Sheets 功能無法使用？

**問題**：部署後發現資料沒有儲存到 Google Sheets。

**解答**：請確認 Google Apps Script 的部署設定：
1. 前往 [Google Apps Script](https://script.google.com/)
2. 開啟您的專案
3. 點選「部署」→「管理部署」
4. 確認「誰可以存取」設為 **所有人**
5. 如果不是，請重新部署並更新 `google-sheets-integration.js` 中的 URL

### Q2: 網站顯示 404 錯誤

**可能原因與解決方法**：

| 原因 | 解決方法 |
|------|---------|
| GitHub Pages 還在建置中 | 等待 3-5 分鐘 |
| 網址錯誤 | 確認網址最後有加 `/index.html` |
| Branch 設定錯誤 | 檢查 Settings → Pages 的 Branch 設定 |

### Q3: 修改程式碼後網站沒有更新

**解決方法**：
1. 確認已經 push 到 GitHub：`git push`
2. 清除瀏覽器快取：`Ctrl+Shift+R` (Windows) 或 `Cmd+Shift+R` (Mac)
3. 檢查 GitHub Actions 是否執行完成（GitHub repository → Actions）

### Q4: 可以設定密碼保護嗎？

**GitHub Pages**：不支援內建的密碼保護

**Cloudflare Pages**：
1. 前往專案設定
2. 啟用 **Access** 功能
3. 設定 Cloudflare Access 原則（需要付費方案）

**替代方案**：在 `index.html` 中加入 JavaScript 密碼檢查（前端驗證，安全性較低）

### Q5: 如何監控網站流量？

#### GitHub Pages
整合 Google Analytics：
1. 前往 [Google Analytics](https://analytics.google.com/)
2. 建立新的資源
3. 複製追蹤代碼
4. 貼到 `index.html` 和 `result.html` 的 `<head>` 區塊

#### Cloudflare Pages
1. 在 Cloudflare Dashboard 點選 **Web Analytics**
2. 啟用分析功能
3. 複製追蹤代碼到網頁中

### Q6: 網站速度太慢怎麼辦？

**優化建議**：
1. ✅ 使用 Cloudflare Pages（自動 CDN 加速）
2. ✅ 啟用 Cloudflare CDN（如果使用自訂網域）
3. ✅ 壓縮圖片檔案
4. ✅ 使用瀏覽器快取

### Q7: 如何備份網站？

**方法 1：GitHub 本身就是備份**
- 所有程式碼都在 GitHub 上
- 可以隨時 clone 下來：`git clone https://github.com/您的使用者名稱/ziwei-calculator.git`

**方法 2：下載 ZIP**
1. 前往 GitHub repository
2. 點選綠色的 **Code** 按鈕
3. 選擇 **Download ZIP**

### Q8: 可以同時部署到兩個平台嗎？

✅ **可以！** 同一個 GitHub repository 可以同時連結到：
- GitHub Pages
- Cloudflare Pages
- Vercel
- Netlify
- 等等...

兩個網站會有不同的網址，但內容相同。

---

## 進階設定

### 設定自動部署

**GitHub Actions 範例**（自動化部署）：

在 repository 中建立 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
```

### 設定環境變數

如果需要在不同環境使用不同的設定（例如不同的 Google Sheets）：

**Cloudflare Pages**：
1. 前往專案設定 → **Environment variables**
2. 新增變數（例如：`SHEET_ID`）
3. 在程式碼中使用環境變數

---

## 安全性建議

### ⚠️ 重要提醒

1. **不要將敏感資訊放在前端程式碼中**
   - ❌ API 金鑰
   - ❌ 資料庫密碼
   - ❌ 私密 Token

2. **Google Sheets Web App URL 的處理**
   - 該 URL 是可以公開的（因為 Google Apps Script 有自己的權限控管）
   - 但建議定期檢查 Google Sheet 的存取記錄

3. **使用 HTTPS**
   - ✅ GitHub Pages 和 Cloudflare Pages 都自動提供 HTTPS
   - ✅ 確保所有外部連結也使用 HTTPS

---

## 效能優化建議

### 1. 啟用 Cloudflare CDN

即使使用 GitHub Pages，也可以透過 Cloudflare CDN 加速：
1. 在 Cloudflare 新增您的網域
2. 將 DNS 指向 Cloudflare
3. 自動啟用 CDN 和快取

### 2. 壓縮檔案

```bash
# 安裝壓縮工具
npm install -g terser clean-css-cli

# 壓縮 JavaScript
terser ziweicore.js -o ziweicore.min.js

# 壓縮 CSS
cleancss -o ziwei.min.css js/ziwei.css
```

### 3. 設定快取標頭

在 Cloudflare Pages 中新增 `_headers` 檔案：

```
/*
  Cache-Control: public, max-age=3600

/*.js
  Cache-Control: public, max-age=86400

/*.css
  Cache-Control: public, max-age=86400
```

---

## 需要協助？

如果在部署過程中遇到問題：

1. 📖 查看本文件的「常見問題」區塊
2. 🔍 檢查瀏覽器 Console 的錯誤訊息
3. 📧 查看 GitHub/Cloudflare 的建置記錄
4. 💬 在 GitHub Issues 中提問

---

## 總結

### 快速選擇指南

**如果您是...**

- 🆕 **第一次部署網站** → 使用 **GitHub Pages**
- ⚡ **需要最快速度** → 使用 **Cloudflare Pages**
- 💰 **預算有限** → 兩者都**完全免費**
- 🌍 **全球訪客** → 使用 **Cloudflare Pages**（全球 CDN）
- 🎯 **簡單快速** → 使用 **GitHub Pages**

### 部署檢查清單

部署前：
- [ ] 確認所有檔案都已上傳
- [ ] 測試本機功能正常
- [ ] Google Sheets 整合已設定完成
- [ ] 移除或替換測試資料

部署後：
- [ ] 測試輸入頁面
- [ ] 測試計算功能
- [ ] 測試 Google Sheets 儲存
- [ ] 在不同裝置上測試（手機、平板、電腦）
- [ ] 檢查 Console 沒有錯誤訊息

---

**祝您部署順利！🎉**
